# UV 
    pip install uv
    cd backend
    uv init
    uv venv
    activate the venv

# download requirements.txt
    uv add -r requirements.txt

# connect to git 

# DVC
    create the dvc.yaml and params.yaml
    dvc init
    dvc repro